name: weight_tracking
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  get: ^4.1.4
  get_storage: ^2.0.2
  intl: ^0.17.0
  uuid: ^3.0.4

flutter:
  uses-material-design: true
