import 'package:flutter/material.dart';

class WeightEntryListItem extends StatelessWidget {
  // TODO: Create weight entry list item
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Weight entry'),
    );
  }
}