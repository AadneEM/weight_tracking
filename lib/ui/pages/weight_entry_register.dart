import 'package:flutter/material.dart';

class WeightEntruRegister extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Weight entry register'),
    );
  }
}
